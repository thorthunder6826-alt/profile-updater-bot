import type { Express } from "express";
import type { Server } from "http";

export async function registerRoutes(
  httpServer: Server,
  app: Express
): Promise<Server> {
  // Amazon clone - static frontend, no API routes needed
  return httpServer;
}
